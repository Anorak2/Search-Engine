Requirements:
- an interface to receive sites from the coordinator so they can be added to the queue
- a method to push discovered sites to the coordinator for approval
- a method to announce a worker to the coordinator so we can receive sites/etc from the coordinator
- a method for pulling pages and extracting all relevant information
- a method for pushing site metadata to the coordinator
